// template strings

const obj = {
  firstName: 'imed',
  lastName: 'jaberi',
  gendre: 'male'
}

console.log(obj.firstName + ' ' + obj.lastName + ' - ' + obj.gendre)
console.log(`${obj.firstName} ${obj.lastName} - ${obj.gendre}`)
